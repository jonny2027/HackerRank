#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main()
{
    int vectorSize = 0;
    int
    cin >> vectorSize;

    vector<int> v(vectorSize);
    for (int i = 0; i < vectorSize; i++)
    {
        cin>>V[i];
    }

    int numOfElementsToRemove - 0;
    cin >> numOfElementsToRemove;

    
    return 0;
}
